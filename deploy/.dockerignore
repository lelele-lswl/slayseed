# ============================================
#  .gitignore 中应该已经排除以下文件
#  这里再额外排除 Docker 相关临时文件
# ============================================

# Docker 镜像构建缓存
node_modules/
target/

# 本地覆盖 docker-compose 的自定义配置
docker-compose.override.yml
.env.local

# 日志
*.log